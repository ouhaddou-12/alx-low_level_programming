#include "main.h"
#include <ctype.h>
#include <stdlib.h>
#include <stdio.h>
/**
 * * add - addition numbers
 * @a: first number
 * @b: second number
 * Return: always 0
*/

int add(int a, int b)
{
int sum;
sum = a + b;
return (sum);
}
