#include "main.h"

/**
 * print_rev -> printing a string in reverse
 * @s: the string to be printed in rev
*/

void print_rev(char *s)
{
	int a = 0;

	while (s[a])
		a++;

	while (i--)
	{
		_putchar(s[i]);
	}
	_putchar('\n');
}
