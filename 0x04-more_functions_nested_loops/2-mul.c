#include "main.h"
#include <ctype.h>

/**
 * mul - mul numbers
 * description: mutli two number
 * @a: first parameter
 * @b: second parameter
 * Return: return 1 if upper otherwise 0
*/
int mul(int a, int b)
{
	int mult;

	mult = a * b;
	return (mult);
}
